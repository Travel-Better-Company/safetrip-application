// user-response.dto.ts
export interface UserResponseDto {
    // Define properties as needed for the user response
    // For example, you might have 'id', 'name', and 'email'.
    id: number;
    name: string;
    email: string;
  }